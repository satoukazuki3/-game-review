<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
    <%= form_with model:@user, local:true do |f| %>
     <h2 class="mt-3">レビューする</h2>
     <%= render 'admin/reviews/form', review: @review %>
      </div>
      <div class='col-md-8 offset-md-1'>
    <h2>Users</h2>
    <%= render 'admin/users/index',users: @users %>
    </div>
    <% end %>
  </div>
</div>
